# Companion Python toolkit for the composite mechanics textbook
# (book proposal package).
#
# Install:
#   python3 -m pip install -r requirements.txt
#
# Tested on Python 3.9+ (macOS, Linux, Windows). The toolkit is pure
# Python and depends only on the three packages listed below — every
# numerical and plotting operation in this package is built on these.

numpy      >= 1.24    # array math, linear algebra, ABD assembly
matplotlib >= 3.7     # through-thickness plots, optimization-history plots
pyyaml     >= 6.0     # ply material definitions in materials/*.yaml
